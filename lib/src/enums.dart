enum AddressComponentTypes {
  plusCode,
  locality,
  subLocality,
  subLocality1,
  subLocality2,
  postalCode,
  country,
  administrativeAreaLevel1,
  administrativeAreaLevel2,
}

enum PinState {
  preparing,
  idle,
  dragging,
}

enum SearchingState {
  idle,
  searching,
}
